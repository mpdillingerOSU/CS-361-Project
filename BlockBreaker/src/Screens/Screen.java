package Screens;

public abstract class Screen {
    /**
     Used to construct the [Screen] within the given [context].
     @param context the context where the instance is to be built
     */
    public abstract void build(BuildContext context);
}
